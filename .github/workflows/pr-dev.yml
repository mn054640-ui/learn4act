name: PR to dev

on:
    pull_request:
        branches:
            - dev

jobs:
    thak-pr:
        runs-on: ubuntu-latest
        steps:
            - name: print thank u message
              run: echo "thanks for pull request"