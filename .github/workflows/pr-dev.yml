name: PR Workflow

on:
    pull_request: 
        branches:
            - dev

jobs:
  say-thanks:
    runs-on: ubuntu-latest

    steps:
        - name: print message
          run: echo "thanks for pull request to dev"